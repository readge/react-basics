import React, { Component } from 'react';

import '../scss/logo.scss';

export default class Logo extends Component {
    render() {
        return <a href="#" className="logo">Netflix Search</a>;
    }
}